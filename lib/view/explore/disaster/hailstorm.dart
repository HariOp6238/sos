import 'package:flutter/material.dart';

class hailstorm extends StatelessWidget {
  const hailstorm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
